<div id="rpicamera-view">
	<h4><i class="fa fa-video-camera"></i> Raspberry Pi camera module</h4>
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-8">
                <div class="width-and-height with-tooltip element">
                    <input type="text" class="width" value="{{usbcamera.width}}"/>
                    <div class="multiply"> x </div>
                    <input class="height" type="text" value="{{usbcamera.height}}"/>
                    <span>
                        <i class="fa fa-question-circle" aria-hidden="true"></i>
                        <span>The resolution of the images being captured by your USB camera. Make sure that this resolution is supported.</span>
                    </span>
                    <i class="description">width</i>
                    <i class="description">height</i>
                </div>
                <div class="slider with-tooltip element">
                    <input class="slider-delay" data-slider-id='ex1Slider' type="text" data-slider-min="0" data-slider-max="2" data-slider-step="0.1" data-slider-value="{{usbcamera.delay}}"/>
                    <i class="description slider">delay in seconds</i>
                    <span>
                        <i class="fa fa-question-circle" aria-hidden="true"></i>
                        <span>The delay time will tell Kerberos.io to wait for x amount of seconds, between two frame captures. This is helpful when you would like to reduce the number of images being processed per second (e.g. for performance reasons).</span>
                    </span>
                </div>

                <div class="slider with-tooltip element">
                    <input class="slider-fps" data-slider-id='ex1Slider' type="text" data-slider-min="0" data-slider-max="30" data-slider-step="1" data-slider-value="{{stream.fps}}"/>
                    <i class="description slider">live stream frames/sec</i>
                    <span>
                        <i class="fa fa-question-circle" aria-hidden="true"></i>
                        <span>The frames per second of the live stream can be configured. Settings this option to zero, will disable the live stream.</span>
                    </span>
                </div>
            </div>
            <div class="col-md-4">
                <div class="rotate with-tooltip element">
                    <div class="image" style="transform:rotate({{usbcamera.angle}}deg)">
                        <i class="fa fa-repeat" aria-hidden="true"></i>
                    </div>
                    <span>
                        <i class="fa fa-question-circle" aria-hidden="true"></i>
                        <span>Click on this image if your camera is rotated. E.g. if your camera is mounted upside down, make sure this image is also upside down.</span>
                    </span>
                </div>
            </div>
        </div>
    </div>
</div>